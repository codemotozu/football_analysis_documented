# Importing the TeamAssigner class from the 'team_assigner' module.
# The '.' before the module name indicates that 'team_assigner' is a relative import,
# meaning it's located within the same package or directory as the current file.
from .team_assigner import TeamAssigner
